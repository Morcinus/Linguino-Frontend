import { IListenIconButton } from "./ListenIconButton";

const base: IListenIconButton = {
  audioLink: "https://samplelib.com/lib/preview/mp3/sample-12s.mp3",
  playOnMount: false,
};

export const mockListenIconButtonProps = {
  base,
};
