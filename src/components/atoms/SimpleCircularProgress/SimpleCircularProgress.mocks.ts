import { ISimpleCircularProgress } from "./SimpleCircularProgress";

const base: ISimpleCircularProgress = {
  progress: 80,
  size: "medium",
  color: "#62B048",
  thickness: "thin",
  children: "🍌Banana",
};

export const mockSimpleCircularProgressProps = {
  base,
};
